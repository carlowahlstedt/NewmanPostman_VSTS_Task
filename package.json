{
  "scripts": {
    "package": "npm run clean  && npm run compile  && tfx extension create  --rev-version",
    "gallery-publish": "tfx extension publish",
    "gallery-publish-rev": "tfx extension publish --rev-version",
    "clean": "rimraf ./*.vsix",
    "compile": "tsc -p .",
    "install-task-lib": "cd NewmanPostman && npm install",
    "pretest": "tsc -p .",
    "test": "npx mocha Tests/TestSuite.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.10.0",
    "tfx-cli": "^0.23.1",
    "typescript": "^5.7.2"
  },
  "name": "vstsexttask",
  "private": true,
  "version": "0.0.1",
  "dependencies": {
    "azure-pipelines-task-lib": "^5.2.10",
    "is-url": "^1.2.4",
    "mocha": "^11.0.0"
  }
}
